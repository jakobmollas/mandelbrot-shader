{
  "name": "mandelbrot-shader",
  "source": "index.html",
  "scripts": {
    "dev": "rimraf cache && rimraf dev && md dev && npm run start-dev-server",
    "build": "rimraf docs && md docs && npm run compile-prod",
    "start-dev-server": "parcel --dist-dir dev",
    "compile-prod": "parcel build --dist-dir docs --no-scope-hoist"
  },
  "license": "ISC",
  "dependencies": {
    "pixi-filters": "^4.1.4",
    "pixi.js": "^6.1.2"
  },
  "devDependencies": {
    "ncp": "^2.0.0",
    "rimraf": "^3.0.2"
  }
}
